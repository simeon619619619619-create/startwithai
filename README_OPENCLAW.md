# startwithai

Local dev:
- cd startwithai
- npm install
- npm run dev -- --port 3010

Build:
- npm run build

Notes:
- Landing is in src/app/page.tsx
- Styling is via Tailwind v4 import in src/app/globals.css
- Lead form currently stores to localStorage key `startwithai_leads` (MVP)
